const pointsEventTarget = new EventTarget();
const selectionEventTarget = new EventTarget();
const modalEventTarget = new EventTarget();
const mockWebEventTarget = new EventTarget();
const mockServerEventTarget = new EventTarget();

const G_EVENT_TARGETS = {

    pointsEventTarget,
    selectionEventTarget,
    modalEventTarget,
    mockWebEventTarget,
    mockServerEventTarget
    
};
export default G_EVENT_TARGETS